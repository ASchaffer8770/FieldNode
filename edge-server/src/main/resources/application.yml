spring:
  application:
    name: fieldnode-edge-server

server:
  port: ${FIELDNODE_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
